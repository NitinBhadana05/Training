<h1 class="text-2xl font-bold mb-4 text-center">Pending Tasks</h1>

<div class="max-w-md mx-auto space-y-3">
  <% @tasks.each do |task| %>
    <div class="p-3 bg-white rounded shadow">
      <%= link_to task.title, task_path(task), class: "text-blue-600" %>
    </div>
  <% end %>
</div>
